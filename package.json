{
  "name": "pixel-art-sprite-editor",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo lint placeholder",
    "test": "node --input-type=module -e \"import('./tests/sprite.test.js').then(()=>console.log('ok')).catch(e=>{console.error(e);process.exit(1)})\""
  }
}
